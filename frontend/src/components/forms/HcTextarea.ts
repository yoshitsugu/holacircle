import styled from 'styled-components';

const HcTextarea = styled.textarea`
  padding: 10px;
  font-size: 1rem;
  border: solid 2px #ccc;
  color: #354052;
  margin: 0 0 20px;
  border-radius: 8px;
  padding: 10px;
  box-shadow: inner 0 0 4px rgba(0, 0, 0, 0.2);
`;

export default HcTextarea;
